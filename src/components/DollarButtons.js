import React from 'react'

const DollarButtons = ({valueAmount}) => {
    return (
        <div id="dollar-values">
            <button>{valueAmount}</button>
        </div>
    )
}

export default DollarButtons
